* Contains python modules developed by Cade in order to interface
with the Mobius chip. 

* `connections_to_bitstream` is a python script developed by PK to
  converted a connections.json file into a bitstream to program the
  Mobius chip. 
